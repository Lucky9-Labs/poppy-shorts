# Install poppy-shorts skills

The installable unit is now the full **poppy-shorts Cursor plugin** (journey capture + preserve-content + inventory tagging).

See **[`docs/INSTALL-PLUGIN.md`](./INSTALL-PLUGIN.md)** for copy-folder, GitHub plugin import, sibling CLI (`--prefix`), and the after-work loop:

```
/poppy-journey → /poppy-preserve-content → /tag-poppy-clips
```

Individual skill folders still live under [`skills/`](../skills/) if you only want to copy one of them into `.cursor/skills`.
